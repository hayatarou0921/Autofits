.trousers.container
  h1 ボトムス一覧
  - @trousers.each do |trouser|
    .item.col-3.d-inline-block.text-center
      =image_tag trouser.image.url
      =link_to '  削除', trouser_path(trouser), method: :delete, data: { confirm: 'ボトムスを削除してもよろしいですか？' }, class: 'btn far fa-trash-alt'
  =link_to 'ボトムスを追加する', new_trouser_path, class: 'btn btn-primary d-block col-4 mx-auto mt-3'
