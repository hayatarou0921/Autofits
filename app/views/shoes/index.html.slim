.shoes.container
  h1 シューズ一覧
  - @shoes.each do |shoe|
    .item.col-3.d-inline-block.text-center
      =image_tag shoe.image.url
      =link_to '  削除', shoe_path(shoe), method: :delete, data: { confirm: 'シューズを削除してもよろしいですか？' }, class: 'btn my-3 far fa-trash-alt'
  =link_to 'シューズを追加する', new_shoe_path, class: 'btn btn-primary d-block col-4 mx-auto mt-3'
