.tops.container
  h1 トップス一覧
  - @tops.each do |top|
    .item.col-3.d-inline-block.text-center
      = image_tag top.image.url, class: 'mx-auto'
      =link_to '削除する', top_path(top), method: :delete, data: { confirm: 'トップスを削除してもよろしいですか？' }, class: 'btn'
  =link_to 'トップスを追加する', new_top_path, class: 'btn btn-primary d-block col-4 mx-auto mt-3'
